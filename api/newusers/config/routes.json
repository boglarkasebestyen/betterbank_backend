{
  "routes": [
    {
      "method": "GET",
      "path": "/newusers",
      "handler": "newusers.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/newusers/count",
      "handler": "newusers.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/newusers/:id",
      "handler": "newusers.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/newusers",
      "handler": "newusers.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/newusers/:id",
      "handler": "newusers.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/newusers/:id",
      "handler": "newusers.delete",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/newusers/login",
      "handler": "newusers.login",
      "config": {
        "policies": []
      }
    }
  ]
}
